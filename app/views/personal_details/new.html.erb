<h1><%= t('page.new_many', model: i18n_model_name(@personal_detail).downcase) %></h1>

<%= render partial: 'form',  locals: { personal_detail: @personal_detail, method: :post, 
  url: user_personal_details_path(@user), submit: t('submit.create_many', model: i18n_model_name(@personal_detail).downcase) } %>
