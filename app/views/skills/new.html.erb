<h1><%= t('page.new_one', model: i18n_model_name(@skill).downcase) %></h1>

<%= render partial: 'form',  locals: { skill: @skill, method: :post, 
  url: user_skills_path(@user), submit: t('submit.create_one', model: i18n_model_name(@skill).downcase) } %>
